import TicketForm from "@/app/(components)/TicketForm";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const TicketPage = ({ params }: { params: any }) => {
  return <TicketForm />;
};

export default TicketPage;
